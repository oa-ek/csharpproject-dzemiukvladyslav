@model BCS.Core.Entities.City

@{
    ViewData["Title"] = "Редагувати обране місто";
}

<div class="container">
    <div class="row justify-content-center mt-3">
        <div class="col-md-10">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h1 class="text-start mb-0">@ViewData["Title"]</h1>
                </div>
                <div class="col-md-6 text-md-end">
                    <a asp-action="Index" asp-controller="City" class="btn btn-outline-secondary mb-0">Скасувати</a>
                </div>
            </div>
            <hr />
            <form asp-action="Update">
                <input type="hidden" value="@Model.Id">

                <div class="form-group mb-3">
                    <label>Назва</label>
                    <div>
                        <input asp-for="Title" value="@Model.Title" class="form-control" id="title" placeholder="Введіть новe назву">
                    </div>
                </div>

                <div class="form-group text-center">
                    <button type="submit" class="btn btn-outline-warning createButtons">Редагувати</button>
                </div>
            </form>
        </div>
    </div>
</div>
